name: spicetify_ui
description: "An unofficial desktop front-end for the Spicetify CLI."
publish_to: 'none'

version: 1.1.1+3

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  window_manager: ^0.5.2
  path: ^1.9.1
  package_info_plus: ^8.3.1
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  test: ^1.31.1

flutter:
  uses-material-design: true
